# Stackr

A declarative homelab Docker Compose deployment tool. Define your self-hosted apps in a single YAML file and let Stackr handle rendering, secret management, validation, and deployment.

Stackr replaces Deployrr (a closed-source PHP/Bash binary) with a fully open, auditable Python implementation.

## Features

- **Declarative config**: one `stackr.yml` drives your entire homelab
- **App catalog**: 11 seed apps across media, management, monitoring, and network categories
- **Secret management**: auto-generated secrets stored in `.stackr.env`, shell env takes priority
- **Pre-deploy validation**: port conflicts, missing secrets, unknown apps, dependency checks
- **State tracking**: stores full compose content for genuine rollback capability
- **Network modes**: `external`, `internal`, or `hybrid` with automatic Traefik label generation
- **Socket proxy**: no app mounts the raw Docker socket when `security.socket_proxy: true`
- **Deep-merge overrides**: apply custom compose keys on top of any catalog template

## Requirements

- Python 3.11+
- Docker Engine 24+ with the Compose plugin (`docker compose`)
- Git (optional, for catalog updates)

## Installation

### One-command (recommended)

```bash
curl -fsSL https://raw.githubusercontent.com/djnw8fs748-eng/deployerreplacement/main/install.sh | bash
```

This installs Stackr via `pipx` into an isolated environment and adds the `stackr` command to your PATH.

### Via pipx

```bash
pipx install git+https://github.com/djnw8fs748-eng/deployerreplacement.git
```

### From source

```bash
git clone https://github.com/djnw8fs748-eng/deployerreplacement.git
cd deployerreplacement
pip install uv
uv pip install -e ".[dev]"
```

## Quickstart

```bash
# 1. Initialise a config directory
stackr init ~/homelab

# 2. Edit the generated config
$EDITOR ~/homelab/stackr.yml

# 3. Validate before deploying
stackr validate ~/homelab/stackr.yml

# 4. Preview what will change
stackr plan ~/homelab/stackr.yml

# 5. Deploy
stackr deploy ~/homelab/stackr.yml
```

## Configuration

Copy `stackr.yml.example` as a starting point:

```bash
cp stackr.yml.example ~/homelab/stackr.yml
```

### Full reference

```yaml
global:
  data_dir: /opt/appdata       # host path for persistent app data
  timezone: Europe/London      # TZ identifier (default: UTC)

network:
  mode: external               # external | internal | hybrid
  domain: example.com          # public domain (external/hybrid)
  local_domain: home.example.com  # LAN domain (internal/hybrid)

traefik:
  enabled: true
  acme_email: you@example.com  # Let's Encrypt registration email
  dns_provider: cloudflare     # DNS challenge provider name
  dns_provider_env:
    CF_DNS_API_TOKEN: ${CF_DNS_API_TOKEN}   # resolved from env / .stackr.env

security:
  socket_proxy: true           # route Docker API through socket-proxy
  auth_provider: authelia      # authelia | authentik | none

apps:
  - name: jellyfin
    enabled: true
    vars:
      hardware_accel: vaapi    # catalog-defined variable
    overrides:                 # deep-merged on top of rendered compose
      services:
        jellyfin:
          mem_limit: "4g"

  - name: radarr
    enabled: true

  - name: sonarr
    enabled: true
```

### Network modes

| Mode | Behaviour |
|------|-----------|
| `external` | Apps exposed only via public domain over HTTPS |
| `internal` | Apps exposed only via local domain (LAN), TLS via DNS challenge |
| `hybrid` | Apps exposed on both public and local domains simultaneously |

## Secret management

Secrets are resolved in this priority order (highest first):

1. **Shell environment** — `export CF_DNS_API_TOKEN=abc123`
2. **`.stackr.env` file** — auto-created by `stackr init`, never committed
3. **Auto-generated** — Stackr generates random secrets for required vars that have no value

`.stackr.env` format:

```
# DO NOT COMMIT THIS FILE
CF_DNS_API_TOKEN=abc123
MY_APP_SECRET=<auto-generated>
```

To set a secret directly:

```bash
stackr secret set CF_DNS_API_TOKEN abc123
```

## CLI reference

```
stackr init <dir>           Initialise a new config directory
stackr validate <config>    Validate config and check for unresolved secrets
stackr render <config>      Render compose files without deploying
stackr plan <config>        Show what would change (diff against current state)
stackr deploy <config>      Validate, render, pull images, and deploy all apps
stackr stop <app>           Stop a running app
stackr restart <app>        Restart a running app
stackr remove <app>         Stop and remove an app's containers
stackr rollback <app>       Redeploy using the last stored compose content
stackr status               Show status of all tracked apps
stackr logs <app>           Stream logs for an app
stackr shell <app>          Open a shell inside the app's primary container
stackr list                 List all catalog apps
stackr search <query>       Search catalog by name or description
stackr update               Update Stackr itself
stackr backup <dir>         Archive config and state to a tarball
stackr restore <archive>    Restore config and state from a backup
stackr catalog update       Pull latest catalog from upstream
stackr catalog version      Show current catalog version
stackr secret set <k> <v>   Set a secret in .stackr.env
stackr secret list          List secret keys (values redacted)
```

## App catalog

### Seed apps

| App | Category | Port |
|-----|----------|------|
| Traefik | network | — |
| Socket Proxy | network | — |
| Jellyfin | media | 8096 |
| Radarr | media | 7878 |
| Sonarr | media | 8989 |
| Prowlarr | media | 9696 |
| qBittorrent | media | 8080 |
| Homepage | dashboard | 3000 |
| Portainer | management | 9000 |
| Uptime Kuma | management | 3001 |
| Grafana | monitoring | 3000 |

### Adding a custom app

Create the following structure inside the catalog:

```
catalog/<category>/<app-name>/
  app.yml           # metadata, ports, volumes, deps
  compose.yml.j2    # Jinja2 compose template
```

Minimal `app.yml`:

```yaml
name: my-app
display_name: My App
description: What it does
category: management
ports: [8080]
traefik_port: 8080
exposure: external
volumes:
  - name: config
    description: App configuration
    container_path: /config
```

Minimal `compose.yml.j2`:

```jinja2
services:
  my-app:
    image: myorg/my-app:latest
    container_name: my-app
    restart: unless-stopped
    volumes:
      - {{ global.data_dir }}/my-app/config:/config
    labels:
      {{ traefik_labels(8080) | items | map('join', ': ') | join('\n      ') }}
    networks:
      - proxy
networks:
  proxy:
    external: true
```

## State and rollback

Stackr tracks every deployed app in `~/.stackr/state.json`. Each entry stores the full rendered compose content alongside a content hash. This enables genuine rollback:

```bash
stackr rollback jellyfin   # redeploys from stored compose content
```

State is written atomically after a successful deploy. No state is written if a deploy fails.

## Development

### Setup

```bash
git clone https://github.com/djnw8fs748-eng/deployerreplacement.git
cd deployerreplacement
python3 -m venv .venv
source .venv/bin/activate
pip install uv
uv pip install -e ".[dev]"
```

### Running tests

```bash
pytest
# with coverage
pytest --cov=stackr --cov-report=term-missing
```

### Linting and type checking

```bash
ruff check stackr tests
mypy stackr
```

### Dependencies

**Runtime:**

| Package | Version | Purpose |
|---------|---------|---------|
| `typer` | >=0.12.0 | CLI framework |
| `pydantic` | >=2.0 | Config schema and validation |
| `jinja2` | >=3.1 | Compose template rendering |
| `pyyaml` | >=6.0 | YAML parsing |
| `rich` | >=13.0 | Terminal output |
| `python-dotenv` | >=1.0 | `.stackr.env` loading |

**Development:**

| Package | Version | Purpose |
|---------|---------|---------|
| `pytest` | >=8.0 | Test runner |
| `pytest-mock` | >=3.14 | Mocking utilities |
| `ruff` | >=0.4 | Linting and formatting |
| `mypy` | >=1.10 | Static type checking |

### Project structure

```
stackr/
  cli.py          Typer CLI entry point
  config.py       Pydantic config models (StackrConfig, AppConfig, ...)
  catalog.py      App catalog loader (CatalogApp, VarDef, VolumeSpec)
  renderer.py     Jinja2 compose renderer + Traefik label generation
  secrets.py      Secret resolution and .stackr.env management
  state.py        State lock file (~/.stackr/state.json)
  deployer.py     Deploy orchestration and rollback
  validator.py    Pre-deploy validation checks

catalog/
  network/traefik/
  network/socket-proxy/
  media/jellyfin/
  media/radarr/
  ...

tests/
  test_config.py
  test_catalog.py
  test_renderer.py
  test_secrets.py
  test_state.py
  test_validator.py
```

## Migrating from Deployrr

1. Run `stackr init ~/homelab` to generate a fresh config
2. Map your existing Deployrr app list to catalog app names (`stackr list`)
3. Copy persistent data directories — `global.data_dir` must point to the same host path
4. Set secrets via `stackr secret set` or export them in your shell
5. Run `stackr validate` and fix any reported issues
6. Run `stackr plan` to preview changes before deploying

## CI

GitHub Actions runs on every push and pull request:

- `ruff check` — linting
- `mypy` — type checking
- `pytest` — full test suite
- Catalog render smoke test — renders all seed apps against a minimal config

## License

MIT
